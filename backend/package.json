{
  "name": "project-root",
  "version": "1.0.0",
  "description": "Root package configuration for managing fullstack project deployment.",
  "main": "index.js",
  "scripts": {
    "install": "npm --prefix backend install",
    "sequelize": "npm run --prefix backend sequelize",
    "start": "npm start --prefix backend",
    "build": "npm run --prefix backend build"
  },
  "license": "ISC"
}